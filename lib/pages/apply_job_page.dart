import 'package:flutter/material.dart';

class ApplyJobPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Sobre Nós'),
      ),
      body: Center(
        child: Text('Esta é a página Sobre Nós'),
      ),
    );
  }
}
